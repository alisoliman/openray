# OpenRay

A native macOS launcher application built with SwiftUI.

## Requirements

- macOS 26.0 or later
- Xcode 26.6 or later

## Generate and build

```bash
xcodegen generate
xcodebuild -project OpenRay.xcodeproj -scheme OpenRay -destination 'platform=macOS' test
```
